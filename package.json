{
  "name": "nr8",
  "version": "0.0.2",
  "private": true,
  "config": {
    "env": "development"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist --force-local",
    "clean": "lerna clean -y && rm -rf ./node_modules",
    "postinstall": "npm run bootstrap",
    "postbootstrap": "npm link @nr8/gateway"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
